You are analyzing a medical receipt/bill PDF document. Your task is to extract EXACT information as it appears on the document.

⚠️ CRITICAL ANTI-HALLUCINATION RULES:
1. NEVER use information from the filename - ONLY read the PDF document
2. NEVER make up, guess, or fabricate ANY information
3. NEVER create fictional addresses - if address is not clearly visible, return "Not Found"
4. NEVER use email addresses as physical addresses
5. If you cannot find specific information on the document, return "Not Found" for that field
6. DO NOT infer or assume - only extract what you can SEE

🚨 CRITICAL: DO NOT USE EXTERNAL KNOWLEDGE 🚨
- DO NOT use your training data, memory, or knowledge of real-world addresses
- DO NOT look up or recall addresses for known businesses (e.g., Costco, CVS, hospitals)
- DO NOT substitute addresses you know from your training data
- ONLY extract the address that is PHYSICALLY PRINTED on this specific receipt
- If you recognize the business name, you must STILL read the address from the receipt, not from your memory
- Example: If you see "Costco" on the receipt, DO NOT recall any Costco addresses you know - READ the address shown on THIS receipt

EXTRACT THE FOLLOWING INFORMATION:

1. **Provider Name**
   - The exact name of the medical provider/facility as shown ON THE RECEIPT
   - Look at the TOP of the receipt, letterhead, or "Bill To" section
   - DO NOT use any names from the filename
   - Examples: "EnVision Eye Care", "Costco Pharmacy", "Sutter Health"
   - If not clearly visible: return "Not Found"

2. **Provider Address**
   - The physical address AS PRINTED on this specific receipt
   - **CRITICAL FORMAT REQUIREMENT**: Output as SINGLE LINE with comma separation
   - **Format structure**: "Street Address, City, State ZIP"
   - DO NOT use newlines or multiline format (even if receipt shows it that way)
   - Look in: letterhead, footer, "Remit To", or contact information section

   ⚠️ **ADDRESS EXTRACTION RULES** ⚠️
   - ONLY extract the address VISIBLE on THIS receipt
   - DO NOT use any addresses from your training data or memory
   - DO NOT "complete" partial addresses using external knowledge
   - If address is PARTIAL (e.g., missing city or ZIP): extract what IS visible and return it as-is
   - If address is COMPLETELY MISSING: return "Not Found"

   **Handling Partial Addresses:**
   - If you see only street address but no city/ZIP: return just the street address
   - If you see only city/state but no street: return just the city/state
   - DO NOT invent or recall the missing parts
   - Example: If receipt shows "1001 Metro Center Blvd" but no city, return: "1001 Metro Center Blvd"

   **What NOT to do:**
   - DO NOT make up addresses like "123 Main St"
   - DO NOT use email addresses
   - DO NOT use phone numbers
   - DO NOT fabricate city names like "Sight City" or "Optics City"
   - DO NOT recall addresses for recognized businesses (e.g., don't use a Costco address you know)

   - Examples of valid addresses (single line, comma-separated):
     * "1241 E Hillsdale Blvd Ste 220, Foster City, CA 94404" (complete address from receipt)
     * "1234 Market Street, San Francisco, CA 94102" (complete address from receipt)
     * "1001 Metro Center Blvd" (partial address - only street visible on receipt)
     * "Foster City, CA 94404" (partial address - only city/zip visible on receipt)

   - Examples of INVALID outputs:
     * Addresses from your training data that aren't on the receipt
     * "contact@provider.com" (email, not address)
     * "1-800-555-1234" (phone number)
     * Made-up addresses to "complete" partial information

3. **Date of Service**
   - The date when medical service was provided
   - Look for "Date of Service", "Service Date", or "DOS"
   - If not found, use the receipt/invoice date
   - Format: YYYY-MM-DD

4. **Amount Paid by Patient (MOST IMPORTANT)**
   - This is the TOTAL amount the PATIENT paid INCLUDING TAX
   - Read the ENTIRE receipt to find the correct amount
   - Look for these terms (in order of priority):
     a) "Patient Responsibility" or "Patient Balance"
     b) "Amount Due" or "Balance Due"
     c) "Total" or "Total Amount" or "Grand Total"
     d) "Amount Paid" or "Payment"
   - INCLUDE sales tax in the amount
   - This should be the FINAL total, not subtotal
   - EXCLUDE amounts paid by insurance
   - CRITICAL: Return ONLY the numeric value without ANY currency symbols, commas, or text
   - Examples of correct format: 21.86, 160.89, 1234.56
   - If you see "$21.86", return: 21.86
   - If you see "1,234.56", return: 1234.56

VERIFICATION CHECKLIST:
Before returning your answer, verify:
- [ ] Did I read the ENTIRE PDF document?
- [ ] Is the provider name from the RECEIPT (not filename)?
- [ ] Is the address EXACTLY as printed on THIS receipt (not from my training data)?
- [ ] Did I avoid using any addresses I know from memory for this business?
- [ ] If the address is partial, did I extract only what's visible (without completing it)?
- [ ] Is the address in SINGLE LINE format: "Street, City, State ZIP" (or partial if that's what's on receipt)?
- [ ] Did I remove any newlines from the address?
- [ ] Is the amount the TOTAL including tax that the patient paid?
- [ ] Did I remove all currency symbols from the amount?

RETURN FORMAT:

You MUST return a valid JSON object with EXACTLY these fields:

{
  "provider": "exact provider name as string",
  "provider_address": "complete or partial address as SINGLE LINE, EXACTLY as printed on receipt (e.g., 'Street, City, State ZIP' or 'Street' if partial) OR 'Not Found'",
  "date_of_service": "date in YYYY-MM-DD format",
  "amount_to_claim": "numeric value only, no currency symbols (e.g., 21.86, 1234.56)"
}

EXAMPLE VALID RESPONSE:
{
  "provider": "EnVision Eye Care",
  "provider_address": "1241 E Hillsdale Blvd Ste 220, Foster City, CA 94404",
  "date_of_service": "2026-01-15",
  "amount_to_claim": "160.89"
}

CRITICAL JSON RULES:
- Return ONLY the JSON object, no other text
- Use double quotes for strings
- date_of_service must be YYYY-MM-DD format
- amount_to_claim must be a plain number (no $, no commas)
- provider_address must be single line with format: "Street, City, State ZIP"
- All fields are required
